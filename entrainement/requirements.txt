# Dépendances du pipeline d'entraînement (Python, hors navigateur).
# mediapipe 0.10.x : MÊME famille que le bundle JS, pour une convention de
# 21 landmarks identique entre entraînement et inférence.
mediapipe==0.10.35
opencv-python-headless>=4.8
numpy>=1.26
scikit-learn>=1.3
matplotlib>=3.7
